# ✅ Java đã được cài đặt thành công!

## 🎉 Bước tiếp theo

### 1. Khởi động lại VS Code (QUAN TRỌNG!)

VS Code cần khởi động lại để nhận diện Java mới cài:

1. **Đóng hoàn toàn VS Code:**
   - Nhấn `Cmd + Q` (không chỉ đóng cửa sổ)
   - Hoặc: VS Code menu → Quit VS Code

2. **Mở lại VS Code**

### 2. Kiểm tra PlantUML Extension

1. Mở VS Code
2. Kiểm tra extension "PlantUML" đã được cài:
   - `Cmd + Shift + X` → Tìm "PlantUML"
   - Nếu chưa có, cài extension "PlantUML" by jebbs

### 3. Sử dụng PlantUML

1. **Mở file diagram:**
   - Mở file `.puml` trong `diagrams/class-diagram/`
   - Ví dụ: `backend-class-diagram.puml`

2. **Preview diagram:**
   - Nhấn `Alt + D` 
   - Hoặc: `Cmd + Shift + P` → "PlantUML: Preview Current Diagram"

3. **Export diagram:**
   - `Cmd + Shift + P` → "PlantUML: Export Current Diagram"
   - Chọn format: PNG, SVG, hoặc PDF

## 📊 Các file diagram có sẵn

Trong thư mục `diagrams/class-diagram/`:

1. **backend-class-diagram.puml** - Backend Class Diagram
2. **frontend-class-diagram.puml** - Frontend Class Diagram  
3. **ai-class-diagram.puml** - AI System Class Diagram
4. **complete-class-diagram.puml** - Complete System Diagram

## 🧪 Test nhanh

Mở file `backend-class-diagram.puml` và nhấn `Alt + D` - bạn sẽ thấy diagram hiển thị!

## 🆘 Nếu vẫn gặp lỗi

1. **Đảm bảo đã khởi động lại VS Code** (Cmd + Q rồi mở lại)
2. Kiểm tra Java: `java -version` trong Terminal
3. Kiểm tra PlantUML extension settings
4. Thử reload VS Code window: `Cmd + Shift + P` → "Developer: Reload Window"

## 🎨 Tips

- **Zoom in/out**: Scroll trong preview window
- **Export**: Có thể export sang PNG để dùng trong tài liệu
- **Edit**: Sửa file `.puml` và preview sẽ tự động cập nhật

